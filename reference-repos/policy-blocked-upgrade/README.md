# Policy blocked demo

The `.savemit-policy.json` manifest deliberately prohibits an automatic Axios upgrade. SAVEMit records `POLICY_BLOCKED` before Docker validation and does not patch the project.
