# Transitive dependency demo

This demo marks `lodash` as supplied through `customer-sdk`. The remediation plan labels it manual, directing the user to update the parent dependency rather than patching the nested package directly.
